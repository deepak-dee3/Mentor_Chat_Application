specVersion: "v1beta"
serviceId: "stores"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "stores-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
